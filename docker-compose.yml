version: "3"
services:
  postgres:
    image: "bitnami/postgresql:latest"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: school
    ports:
      - "5432:5432"
